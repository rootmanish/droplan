/**
 * Single source of truth for every external link on the site.
 *
 * The owner/repo below was resolved from this repository's own `origin`
 * remote (`git@rootmanish:rootmanish/droplan.git`, an SSH host alias that
 * points at `github.com`), not guessed.
 */
export const REPO_URL = "https://github.com/rootmanish/droplan";
export const RELEASES_URL = `${REPO_URL}/releases/latest`;
export const ISSUES_URL = `${REPO_URL}/issues`;
export const README_URL = `${REPO_URL}#readme`;
export const LICENSE_URL = `${REPO_URL}/blob/main/LICENSE`;
export const SECURITY_MODEL_URL = `${REPO_URL}#security-model`;
export const BUILD_FROM_SOURCE_URL = `${REPO_URL}#building-it`;

export const SITE_URL = "https://droplan.devopsinfo.in";
